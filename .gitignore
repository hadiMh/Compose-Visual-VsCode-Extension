# Dependencies
node_modules/

# TypeScript / build (compiled extension host code)
out/
dist/
build/
*.tsbuildinfo

# VS Code extension packaging & integration tests
*.vsix
.vscode-test/
.vscode-test-user-data/

# Logs & debug
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Test / coverage
coverage/
.nyc_output/
*.lcov

# Environment & secrets (never commit)
.env
.env.*
!.env.example
*.pem
*.key
*.p12
credentials.json
secrets.json

# Runtime data when dogfooding this extension locally
.composeVisual/

# Local marketplace / promo scratch (screenshots, recordings, editor projects)
# Commit only curated assets under assets/ (e.g. icon.png).
publish media/

# OS
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db
ehthumbs.db
Desktop.ini

# Editor / IDE — keep shared VS Code config; ignore personal settings
.vscode/*
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/extensions.json
.idea/
*.swp
*.swo
*~

# Cursor / Claude local agent tooling
.cursor/
.claude/

# Tool caches
.eslintcache
.prettiercache
.cache/
tmp/
temp/
